# Feedback batch A (15 September) — implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Four small, explicit asks from the owner's 15 September feedback: drop the "needs email configured" copy; make error toasts dismissable; let permitted absence periods be edited and carry a maximum stay; give the admin residents view room on a desktop.

**Architecture:** Front-end copy and CSS in `public/admin.html` / `public/app-common.js` / `public/app-common.css`; one migration (050) adding `absence_windows.max_nights` plus a `PATCH` route and an edit affordance; the holiday warning in `routes/residents.js` learns the maximum. No new dependencies.

**Tech Stack:** Express + Postgres 16, numbered migrations, `tenant/template.sql` regenerated by `tools/gen-tenant-template.sh`, plain-assert suites via `./check.sh` (`PGBIN=/opt/homebrew/opt/postgresql@16/bin`).

The owner's words, for the record: *"remove the needs email configured on the service copy everywhere, it's all configured now"*; *"Give the reason for the export toast still shows for ages even after the reason is given, it's not clear how to dismiss"*; *"Permitted absence periods need to have a setting for max consecutive nights, e.g. Easter/Ramadan 2026 … no more than 2 weeks"*; *"I had a typo in permitted absence date and can't edit it"*; *"permitted absence screen needs to be updated"*; *"When I'm on the desktop, the resident view should fill the whole screen more"*.

## Global Constraints

- The inline `<script>`/`<style>` in `public/*.html` are hashed into the CSP at boot; edit them freely, but no `style=""` attributes and no third-party sources. `check.sh` layer 1 parses every inline script.
- Every user-visible error is a sentence for a manager. Comments match the surrounding density and say why.
- Migration 050 must be followed by `PGBIN=/opt/homebrew/opt/postgresql@16/bin ./tools/gen-tenant-template.sh`; commit the regenerated `tenant/template.sql`. Migrations only add — nothing dropped (rolling deploy).
- `test/permissions.js` is the single source of `docs/PERMISSIONS.md` (`node tools/gen-permissions-doc.js`, `--check` in check.sh); any new route gets a row.
- Commit after every task with the footer:
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01FofqrXrLKdxybZvZYupwRB
  ```

---

### Task 1: Drop the "needs email configured" copy

**Files:** `public/admin.html:505,506,509` (the three Settings labels) and `:1988` (the send-now failure toast); `README.md:615`.

- [ ] **Step 1:** In the three `<label class="check">` spans, delete the sentence `Needs email configured on the service.` (and the space before it). Check `grep -n "email configured" public/*.html README.md docs/*.md` afterwards and remove any remaining instance in user-facing copy (README "Needs email configured." at ~615 becomes nothing, or "Email is configured on the service." if the sentence needs a subject). Leave code comments and `lib/mail.js` warnings alone — they are for operators.
- [ ] **Step 2:** The toast at `:1988` names environment variables at a manager. Change to: ``toast(`Nothing was delivered for ${out.from} to ${out.to} — the mail service did not accept it. Try again in a minute, or tell whoever runs the service.`, "err")``.
- [ ] **Step 3:** `./check.sh 2>&1 | head -12` — layer 1 green. Commit: `Settings: email is configured; stop saying it needs to be`.

---

### Task 2: Error toasts can be dismissed

**Files:** `public/app-common.js:68-80` (`toast()`), `public/app-common.css:719-728`; `public/admin.html` (the reason fields `exReason`, `erReason`, `rpReason`), `public/checkin.html`/`app-common.js:672` (the export form's `reason`).

Today an `"err"` toast sets `role=alert` and never times out; nothing dismisses it but the next toast. The owner typed the reason and the toast stayed.

- [ ] **Step 1:** In `toast()`: keep `role=alert` for errors but give them a timeout too — 6000 ms (long enough to read, short enough not to linger); `ok` stays 3200. Export a `dismissToast()` (`el.className = ""; clearTimeout(toast._t)`) and bind once, next to `toast()`'s definition: `$("toast").addEventListener("click", dismissToast)` — guard for `$("toast")` being absent on pages without one. Add `cursor: pointer;` to `#toast` in the CSS and a `title="Dismiss"` on the `#toast` element wherever it is declared (`grep -n 'id="toast"' public/*.html`).
- [ ] **Step 2:** The reason fields: on `input`, if the visible toast is an error, dismiss it. One helper in `app-common.js`: `function dismissToastOnInput(el) { el?.addEventListener("input", () => { if ($("toast").classList.contains("err")) dismissToast(); }); }` and call it for `exReason`, `erReason`, `rpReason` in admin.html's wiring section and for the export form's reason in the register page (find where `exportForm` is defined).
- [ ] **Step 3:** `./check.sh 2>&1 | head -12` — layer 1 green; open the admin page in a browser if convenient and confirm a click clears the toast. Commit: `Toasts: errors time out, a tap dismisses, typing the reason clears "give the reason"`.

---

### Task 3: Permitted absence periods — edit, and a maximum stay

**Files:** create `migrations/050_absence_window_max_nights.sql`; regenerate `tenant/template.sql`; modify `routes/settings.js:221-260` (add `PATCH /absence-windows/:id`, accept `max_nights` on POST); `routes/residents.js:384-392` (the warning); `public/admin.html:514-521` (form) and `:1993-2015` (handlers); `test/permissions.js` (row for PATCH); `test/api.test.js` (~2541, the absence-windows block); `docs/PERMISSIONS.md` regenerated.

**Interfaces:**
- `absence_windows.max_nights integer null check (max_nights between 1 and 365)` — null means no maximum.
- `POST /api/settings/absence-windows { name, from_date, to_date, max_nights? }`; `PATCH /api/settings/absence-windows/:id { name?, from_date?, to_date?, max_nights? }` (admin; 404 for a missing id; 400 with the constraint's meaning for bad dates); `GET` returns `max_nights` on every row.
- Warning text on `POST /api/residents/:id/absences` for a holiday: outside every window → existing text; inside a window whose `max_nights` is set and the span (inclusive nights, `to - from + 1`) exceeds it → `Longer than the ${max_nights} nights permitted for ${name}`.

- [ ] **Step 1: Failing tests.** In the api absence-windows block: (a) POST with `max_nights: 14` → GET shows 14; (b) PATCH `to_date` and `name` → GET shows both; PATCH `max_nights: null` clears it; PATCH by a supervisor → 403; PATCH a missing id → 404; PATCH `to_date` before `from_date` → 400 with a sentence; (c) the warning: create a window `2030-04-18..2030-05-02` with `max_nights 14`; authorise a holiday `2030-04-18..2030-04-25` → no warning; `2030-04-18..2030-05-02` (15 nights) → warning matching `/Longer than the 14 nights permitted for/`; `2030-06-01..2030-06-03` → the existing "Outside the permitted absence periods" warning. Reuse the existing test's admin client and a resident created in that block. Run the suite → fails on `max_nights` undefined.
- [ ] **Step 2: Migration 050.** `alter table public.absence_windows add column if not exists max_nights integer check (max_nights between 1 and 365);` with a comment: the IPAS circular's wording ("no more than 2 weeks" for Ramadan/Easter) is a maximum per period, so it lives on the period; null = no maximum; the register warns, never refuses. Regenerate the template.
- [ ] **Step 3: Routes.** Extend POST to read `max_nights` (integer 1–365 or null; anything else → 400 "Maximum nights must be a whole number between 1 and 365, or left blank"). Add PATCH: admin check like the siblings, build the `set` list from the provided keys only, `returning *`, 404 when no row. Translate `23514` from the date check into "The last day must be on or after the first day." Add the permissions row: `{ area: 'Administration', name: 'Edit a permitted absence period', method: 'PATCH', path: (fx) => \`/api/settings/absence-windows/${fx.absenceWindowId}\`, body: () => ({ name: 'Edited' }), expect: ADMIN, fresh: 'absenceWindow' }`. Regenerate `docs/PERMISSIONS.md`.
- [ ] **Step 4: The warning.** In `routes/residents.js` replace the two-column query with one that also finds the tightest containing window: `select (select count(*)::int from absence_windows) as n, (select name from absence_windows w where daterange(w.from_date, w.to_date, '[]') @> daterange($1, $2, '[]') order by (w.to_date - w.from_date) limit 1) as inside_name, (select max_nights from absence_windows w where … same … limit 1) as max_nights`. Then: `if (w.n > 0 && !w.inside_name) warning = 'Outside the permitted absence periods in Settings'; else if (w.max_nights && nights > w.max_nights) warning = \`Longer than the ${w.max_nights} nights permitted for ${w.inside_name}\`;` where `nights` is the inclusive day count computed from `from`/`to` (they are ISO strings; compute in SQL as `$2::date - $1::date + 1` in the same query to avoid JS date maths).
- [ ] **Step 5: The screen.** Form gains a fourth field `awMax` (`type="number" min="1" max="365" placeholder="Max nights (optional)"`) — change `grid3` to a 4-column grid (add `.grid4` in the page's `<style>` mirroring `grid3`, stacking on narrow screens). Each list row shows `name — from to to — up to N nights` (omit the last part when null) and gains an `edit` linkish button beside `remove`. Edit loads the row into the four fields, changes the button text to `Save changes`, shows a `Cancel` ghost button, and stores the editing id on the form (`data-editing`); Save PATCHes, Cancel clears. The tooltip on the heading gains: "Each period can carry a maximum stay — the IPAS wording for Ramadan and Easter is no more than two weeks." Toasts: `Period saved`.
- [ ] **Step 6:** `PGBIN=... ./check.sh` all green. Commit: `Permitted absence periods can be edited and carry a maximum stay (migration 050)`.

---

### Task 4: Room on a desktop

**Files:** `public/admin.html` `<style>` block (and `public/app-common.css` only if a shared rule must change).

Today every page's `.wrap` is capped at 760px (`app-common.css:81,91,165`). On a monitor the residents list is a narrow column.

- [ ] **Step 1:** In `public/admin.html`'s `<style>`, add a `@media (min-width: 1100px)` block scoped to this page: `.wrap`, the header's inner wrap and the tab bar's wrap go to `max-width: 1180px`; the residents `.list` becomes `display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px;` with `.list .letter { grid-column: 1 / -1; }` so the A–Z headings span; the staff and buildings lists may share the two-column rule if they use `.list` (check — if `.card.staff` looks wrong two-up, exclude it with a `:has()`-free selector such as giving the residents list container an id and scoping to it). Detail/edit sheets keep their width.
- [ ] **Step 2:** Check in a browser at ≥1100px and at phone width (nothing changes below the breakpoint). `./check.sh 2>&1 | head -12`. Commit: `Admin: two columns and a wider page on a desktop`.

---

## Self-review

Four owner asks → four tasks. Task 3 covers three of the owner's lines (max nights, the uneditable typo, "screen needs to be updated"). No placeholders: each step names the file, the field, the text. Interfaces are pinned (column, routes, warning text). Task 4 is deliberately modest — the owner can push it further after seeing it.
