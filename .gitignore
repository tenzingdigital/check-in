.DS_Store
node_modules/

# Local environment: DATABASE_URL and anything else the service needs to run
# off Render. This is a real secret now — the connection string is the whole
# database — unlike the Supabase anon key this file used to hold.
.env
.env.*
# …but the example is documentation and belongs in the repo.
!.env.example

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
